package com.nimba.analysissheet

/**
 * The Fiche d'analyse's top-level groups, rendered as one tab each. Pilier 2
 * (contrat/marché, entirely variant-specific) is deliberately absent — it is
 * fast-follow work, not yet built alongside this section framework's proof set.
 */
enum class FaPilier {
    COVER,
    PILIER_1,
    PILIER_3,
    PILIER_4,
    CONCLUSION,
}
